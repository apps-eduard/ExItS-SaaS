/**
 * Canonical Lucide icon map for ExItS semantic actions.
 * Prefer importing from here instead of picking alternate icons per feature.
 *
 * @see action-semantics.ts
 * @see Docs/UI/EXITS_UI_STANDARD.md
 */
export {
  EXITS_ACTIONS,
  EXITS_ACTION_ICON_ALIASES,
  getActionIcon,
  getActionIntent,
  getActionButtonStyle,
  type ExitsActionId,
  type ExitsActionDefinition,
  type ActionButtonStyle,
  type ButtonIntent,
} from "@/components/exits/action-semantics";
