@page "/reports/expenses"
@using ExItS.PinoyBusinessPOS.Application.Commercial
@using ExItS.PinoyBusinessPOS.Application.Reporting
@inject NavigationManager Nav
@inject NavigationGate Gate
@inject IPosReportingClient Reports
@inject IUtangCapabilityEvaluator Capabilities
@inject ApiStatusLocalizer ApiLocalizer
@inject IConnectivityService Connectivity
@inject IStringLocalizer<PosResources> L

@if (!_ready)
{
    <Page><Spinner /></Page>
}
else
{
<Page>
    <PageHeader Title="@L["Reports_Expenses"]" Subtitle="@L["Reports_ExpensesSubtitle"]">
        <Actions>
            <Button Variant="ButtonVariant.Secondary" OnClick="GoHub">@L["Reports_Back"]</Button>
            <Button Variant="ButtonVariant.Secondary" IsLoading="@_loading" OnClick="ReloadAsync">@L["Dashboard_Refresh"]</Button>
        </Actions>
    </PageHeader>

    @if (_isOffline)
    {
        <Alert Tone="AlertTone.Warning" Title="@L["Dashboard_OfflineTitle"]" Message="@L["Dashboard_OfflineMessage"]" />
    }

    @if (!string.IsNullOrEmpty(_errorMessage))
    {
        <Alert Tone="AlertTone.Danger" Title="@(_errorTitle ?? L["Dashboard_ErrorTitle"])" Message="@_errorMessage" />
    }

    <Section Title="@L["Dashboard_FiltersTitle"]">
        <Stack Gap="3">
            <DateInput Label="@L["Dashboard_FromDate"]" Value="@_fromDate" ValueChanged="OnFromDateChanged" />
            <DateInput Label="@L["Dashboard_ToDate"]" Value="@_toDate" ValueChanged="OnToDateChanged" />
        </Stack>
    </Section>

    @if (_report is not null)
    {
        <Section Title="@L["Reports_SummaryTitle"]">
            <Stack Gap="3">
                <div>
                    <span class="pos-muted">@L["Reports_ActiveExpenses"]</span>
                    <div class="pos-value-md"><MoneyDisplay Amount="@_report.ActiveExpenseTotal" CurrencyCode="PHP" /></div>
                    <div class="pos-muted">@_report.ActiveExpenseCount</div>
                </div>
                <div>
                    <span class="pos-muted">@L["Reports_VoidedExpenses"]</span>
                    <div class="pos-value-md"><MoneyDisplay Amount="@_report.VoidedExpenseTotal" CurrencyCode="PHP" /></div>
                    <div class="pos-muted">@_report.VoidedExpenseCount</div>
                </div>
            </Stack>
        </Section>

        <Section Title="@L["Reports_ByCategory"]">
            <Stack Gap="3">
                @foreach (var row in _report.ByCategory)
                {
                    <div class="pos-row">
                        <span class="pos-value-md">@(row.CategoryName ?? row.CategoryId.ToString("D"))</span>
                        <span class="pos-muted">@row.Count</span>
                        <MoneyDisplay Amount="@row.TotalAmount" CurrencyCode="PHP" />
                    </div>
                }
            </Stack>
        </Section>

        <Section Title="@L["Reports_ExpenseDetails"]">
            <Stack Gap="3">
                @foreach (var row in _report.Details.Take(50))
                {
                    <div class="pos-row" @onclick="() => GoExpense(row.ExpenseId)">
                        <span class="pos-value-md">@row.ExpenseNumber</span>
                        <span class="pos-muted">@row.Status</span>
                        <MoneyDisplay Amount="@row.Amount" CurrencyCode="PHP" />
                    </div>
                }
            </Stack>
        </Section>
    }
    else if (_loading)
    {
        <Spinner /><span>@L["Dashboard_Loading"]</span>
    }
</Page>
}

@code {
    private bool _ready;
    private bool _loading;
    private bool _isOffline;
    private string? _errorTitle;
    private string? _errorMessage;
    private DateOnly? _fromDate;
    private DateOnly? _toDate;
    private PosExpensesReportDto? _report;

    protected override async Task OnInitializedAsync()
    {
        if (!Gate.CanEnterProtectedShell || !Capabilities.IsAllowed(UtangCapability.ViewReports))
        {
            Nav.NavigateTo("/home", replace: true);
            return;
        }

        var today = DateOnly.FromDateTime(DateTime.UtcNow);
        _fromDate = today;
        _toDate = today;
        _ready = true;
        await ReloadAsync();
    }

    private async Task OnFromDateChanged(DateOnly? value) { _fromDate = value; await ReloadAsync(); }
    private async Task OnToDateChanged(DateOnly? value) { _toDate = value; await ReloadAsync(); }

    private async Task ReloadAsync()
    {
        _loading = true;
        _errorTitle = null;
        _errorMessage = null;
        _report = null;
        _isOffline = !await Connectivity.IsConnectedAsync();
        if (_isOffline) { _loading = false; return; }

        var result = await Reports.GetExpensesReportAsync(_fromDate, _toDate);
        if (result.IsSuccess) { _report = result.Data; }
        else
        {
            var described = ApiLocalizer.Describe(result.Status, result.Error);
            _errorTitle = described.Title;
            _errorMessage = result.Error?.Detail ?? described.Message;
        }
        _loading = false;
    }

    private void GoHub() => Nav.NavigateTo("/reports");
    private void GoExpense(Guid expenseId) => Nav.NavigateTo($"/expenses/{expenseId:D}");
}
