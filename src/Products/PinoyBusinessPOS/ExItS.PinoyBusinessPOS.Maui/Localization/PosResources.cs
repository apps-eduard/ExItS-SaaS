namespace ExItS.PinoyBusinessPOS.Maui.Localization;

/// <summary>
/// Marker type used only as the generic argument for <c>IStringLocalizer&lt;PosResources&gt;</c>.
/// Localized strings live in <c>PosResources.resx</c> (en) and
/// <c>PosResources.fil-PH.resx</c> (fil-PH) in this same folder; there is no Designer.cs so the
/// resource keys in those files are the source of truth.
/// </summary>
public sealed class PosResources
{
    private PosResources()
    {
    }
}
