namespace ExItS.Platform.Domain.Entitlements;

public enum EntitlementGrantSource
{
    Plan = 1,
    Trial = 2,
    Override = 3
}

public enum FeatureOverrideStatus
{
    Active = 1,
    Revoked = 2
}
