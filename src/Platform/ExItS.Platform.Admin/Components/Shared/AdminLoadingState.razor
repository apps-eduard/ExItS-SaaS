@inject IStringLocalizer<AdminResources> L

<div class="admin-loading-state" style="padding:2rem;text-align:center;" role="status" aria-live="polite">
    <Spin Tip="@(Message ?? L["Common_Loading"].Value)" />
</div>

@code {
    [Parameter] public string? Message { get; set; }
}
