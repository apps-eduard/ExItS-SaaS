@inject IStringLocalizer<AdminResources> L

<p class="state loading" role="status" aria-live="polite">
    <span class="spinner" aria-hidden="true"></span>
    @(Message ?? L["State_Loading"])
</p>

@code {
    [Parameter] public string? Message { get; set; }
}
