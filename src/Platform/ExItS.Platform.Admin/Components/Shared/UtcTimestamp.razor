@*
  Friendly local date/time for Admin surfaces (standards §12).
  UTC remains in the tooltip for operators; primary text is never "… UTC".
*@
<LocalTimestamp Value="@Value" />

@code {
    [Parameter] public DateTimeOffset? Value { get; set; }
}
